# History 路由部署（npm run build:history）
#
# 适用：需要干净 URL（无 #）且**服务端已配置 SPA fallback**
# （所有未匹配路由都返回 index.html，例如 nginx 的 try_files $uri /index.html）。
# 未配 fallback 时，直接访问 /some/route 会 404、刷新页面白屏——此时请用默认或 embed 模式。

VITE_ROUTER_MODE=history
VITE_BASE=/
VITE_BUILD_TARGET=standard
