{
  "name": "pangea-app-starter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "type-check": "vue-tsc --noEmit",
    "build": "vue-tsc --noEmit && vite build",
    "build:embed": "vue-tsc --noEmit && vite build --mode embed",
    "build:history": "vue-tsc --noEmit && vite build --mode history",
    "preview": "vite preview",
    "check:tokens": "node scripts/check-tokens.mjs",
    "gate": "node scripts/check-tokens.mjs && vue-tsc --noEmit && vite build",
    "gate:embed": "node scripts/check-tokens.mjs && vue-tsc --noEmit && vite build --mode embed"
  },
  "dependencies": {
    "@arco-design/web-vue": "^2.57.0",
    "@arco-iconbox/vue-pangea-mobile": "^1.0.24",
    "@arco-themes/vue-pangea-3-linear": "^1.0.11",
    "vue": "^3.4.0",
    "vue-router": "^4.3.0"
  },
  "devDependencies": {
    "@arco-plugins/vite-vue": "^1.4.5",
    "@vitejs/plugin-vue": "^5.0.0",
    "less": "^4.2.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vite-plugin-singlefile": "2.3.3",
    "vue-tsc": "^2.0.0"
  }
}
