# 嵌入式单文件部署（npm run build:embed）
#
# 适用：飞书 aily（开发类任务由妙搭执行）/ 妙搭 Miaoda / Coze / 任何 iframe 嵌入
# 或"给我一个 HTML 双击就能打开"的场景。
#
# 为什么这套配置能治白屏（三层根因逐一消除）：
#   1. Hash 路由      → 不需要服务端 SPA fallback，深链接不会 404
#   2. 相对 base      → 不假设部署在域名根，子路径（如 /page/<token>/）也能取到资源
#   3. 单文件全内联   → 没有任何外部 JS/CSS 请求，也没有动态 chunk 需要解析路径；
#                       即使平台注入动态 <base> 标签也不受影响
#
# 代价：产物较大（约 3~4MB，gzip 约 1MB）。若安装了 @visactor/vchart，会再显著增大。

VITE_ROUTER_MODE=hash
VITE_BASE=./
VITE_BUILD_TARGET=embed
